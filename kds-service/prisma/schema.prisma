// Minimal Prisma schema for KDS (no models needed, but for consistency)
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
