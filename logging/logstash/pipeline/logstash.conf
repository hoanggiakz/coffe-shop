input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_field => { "service" => "coffee-shop" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "coffee-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
