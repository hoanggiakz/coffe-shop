# Apps Layout

Thư mục `apps/` gom toàn bộ ứng dụng chạy chính:

- `apps/backend/`: các microservice backend
- `apps/frontend/`: ứng dụng web frontend

Mục tiêu: tách rõ code ứng dụng khỏi hạ tầng (`ops/k8s/`, `ops/scripts/`, `ops/docs/`), giúp dự án gọn và dễ điều hướng.


